This folder will contain the trained `phishing_model.pkl` and `scaler.pkl` after running `python ml/train_model.py`.

If you want to include a prebuilt model, you can place `phishing_model.pkl` here.
